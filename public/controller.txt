<!DOCTYPE html>
<html>
<head>
  <title>Controller</title>
  <style>
    button {
      font-size: 24px;
      padding: 20px;
      margin: 5px;
    }
  </style>
</head>
<body>
  <h2>Controller</h2>

  <div>
    <button onclick="send('up')">⬆️</button>
  </div>
  <div>
    <button onclick="send('left')">⬅️</button>
    <button onclick="send('select')">⭕</button>
    <button onclick="send('right')">➡️</button>
  </div>
  <div>
    <button onclick="send('down')">⬇️</button>
  </div>

  <script>
    const room = "room1";
    const ws = new WebSocket(`ws://${location.host}`);

    ws.onopen = () => {
      ws.send(JSON.stringify({
        type: "join",
        room,
        role: "controller"
      }));
    };

    function send(action) {
      ws.send(JSON.stringify({
        type: "control",
        action
      }));
    }
  </script>
</body>
</html>
